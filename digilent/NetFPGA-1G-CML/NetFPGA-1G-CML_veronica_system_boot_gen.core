CAPI=2:

name: AFRL:utility:digilent_veronica_NetFPGA-1G-CML_boot_gen:1.0.0
description: Git pull openSBI and uboot, generate payload bin file, and copy to BOOTFS folder in build.

filesets:
  src:
    files:
      - NetFPGA-1G-CML_veronica_boot.yml : {file_type: user, copyto: ./BOOTFS/bootfs.yml}

  dep:
    depend:
      - AFRL:utility:veronica_boot_gen:1.0.0

targets:
  default: &default
    filesets: [src, dep]
